export type TokenType = {
  id: string;
  name: string;
  email: string;
  image: string;
  role: string;
  provider: string;
}

export type UserType = {
  id: string;
  name: string;
  email: string;
  image: string;
  role: string;
  provider: string;
}
